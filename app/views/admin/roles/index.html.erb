<table class="table table-border">
  <thead>
    <tr>
      <th>
        <%= "Name" %>
      </th>
    </tr>
  </thead>
  <tbody>
    <% @roles.each do |role| %>
      <tr>
        <td>
          <%= link_to role.name, [:admin, role] %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
